{
  "tasks": {
    "lock": "deno cache --lock=deno.lock --lock-write ./*.ts git/*.ts",
    "fmt": "deno fmt",
    "lint": "deno lint",
    "test": "deno test -A --no-check",
    "install": "deno install -A --name molt cli.ts"
  }
}
